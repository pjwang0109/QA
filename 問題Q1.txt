在暫存按鈕中有一個 saveOpData function，在傳送按鈕中有一個 send function，我想在按下傳送按鈕時，可以觸發暫存按鈕，只要成功，再接續做傳送按鈕的其他事情，這個程式碼該怎麼改?

saveOpData function 在 btnProcess.js裡，而 send function 在 Branch.js裡

傳送按鈕:
send: async function (obj) {
  loadingShow("資料送出中");
  程式碼內容...
  loadingHide();
}


暫存按鈕:
async function saveOpData(target, showMsg) {
    let data = await saveOpJSON(target);
    let dataArr = [];
    dataArr.push(data);
    //儲存作業中心資料。
    let dataAll = "";

    if ($(".btnOpDiv").attr("cftype") == "11") {
        dataAll = getS060001037(uid, token, branch, data);
    } else {
        dataAll = getS060001032(uid, token, data);
    }

}


async function saveOpJSON(obj) {
  程式碼內容...
}


網頁中有一個暫存按鈕，和傳送按鈕
我想在按下傳送按鈕(send)時，可以觸發暫存按鈕(saveOpData)，只要成功，再接續做原本傳送按鈕的其他事情，這個程式碼該怎麼改?

