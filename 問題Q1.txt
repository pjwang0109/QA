我如果有很多api 可以使用以下方法去做嗎
try {
                    res = await Promise.all([
                        apiS120010001(uid, token, crpt_pk),
                        apiS120010002(uid, token, crpt_pk),
                        ...
                        ...
                        ...
                    ]);
                } catch (e) {
                    console.error(e);
                    return;
                }
