 當我按下addHostTranCoData 按鈕時，我想要從 openOpSendDiv拿到 cid的值來用，怎麼寫?

async function openOpSendDiv(obj) {
    const userObj = getUidToken();
    const uid = userObj.uid;
    const token = userObj.token;
    let rowTable = $(obj).closest("table").DataTable();
    let tr = $(obj).closest("tr");
    let cid = tr.find('td:eq(6)').text().trim();

}


async function addHostTranCoData(obj) {
    const $btn = $(obj);
    const $div = $btn.closest("#transWorkCoDiv");


    try {
        const res = await Promise.all(
            [
                apiS110010018(uid, token, cid, idCode),
                $.get(ajaxhttpIPHostHtml("/acceptcase/creditCheckReport/page/component/queryCreditCheckDataModal.html"))
            ]);
        jsonData = res[0];
        html = res[1];
    } catch (e) { console.error(e) }
}
