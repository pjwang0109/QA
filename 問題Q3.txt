這是他原本寫的 可以幫我用他的方法改寫嗎
const classContractsArr = [".no", ".acc", ".clause", ".clauseType", ".year", ".month", ".day", ".quality",
            ".qualityType1", ".qualityCurrency1", ".qualityType2", ".qualityCurrency2", ".qualityType3", ".qualityCurrency3",
            ".qualityType4", ".qualityCurrency4", ".otheClause", ".guarantor", ".responsible", ".seal", ".sign", ".seal2" ,
            ".pledgeSeal1", ".pledgeSeal2"];
        const contractsArr = new Array;

        $this.find("#contractsDiv").find(".contracts").each(function (item, thisContracts) {
            const contractsObj = new Object;
            let clauseArr = new Array;
            let clause = "";
            let guaArr = new Array;
            let resArr = new Array;
            $.each(classContractsArr, function (item, classContracts) {
                const dataContracts = classContracts.replace('.', '');
                let classVal = $(thisContracts).find(classContracts).val() || "";
                switch (dataContracts) {
....
case "guarantor":  //保證人擔保金額
                        $this.find(`.${dataContracts}`).each(function () {
                        console.log('QQ@@@', $(this));
                            let code = $(this).find(".code").val() || "";
                            let currencyType = $(this).find(".guaCurrency").val() || "";
                            let name = $(this).find(".gccMoney").val() || "";
                            let checkbox = $(this).find(".checkbox").val() || "";
                            let clauseType = $(this).find(".clauseType").val() || "";
                            let year = $(this).find(".year").val() || "";
                            let month = $(this).find(".month").val() || "";
                            let day = $(this).find(".day").val() || "";
                            let guaObj = { "code": code, "currencyType": currencyType, "name": name, 
                                "checkbox": checkbox, "clauseType": clauseType, "year": year, "month": month, "day": day };
                            guaArr.push(guaObj);
                        })
                        contractsObj[dataContracts] = guaArr;
                        break;

....
