我現在要寫暫存的資料
"checkbox": checkbox, "clauseType": clauseType, "year": year, "month": month, "day": day 是新增的 
但是我這樣寫好像有問題 因為她有分區塊
我要儲存的格式是
 "guarantor":{
         "checkbox":"",//顯示保證人擔保額度
         "clauseType":"",//期間保證
         "year":"",
         "month":"",
         "day":""
     }
這要怎麼改

case "guarantor":  //保證人擔保金額
                        $this.find(`.${dataContracts}`).each(function () {
                        console.log('QQ@@@', $(this));
                            let code = $(this).find(".code").val() || "";
                            let currencyType = $(this).find(".guaCurrency").val() || "";
                            let name = $(this).find(".gccMoney").val() || "";
                            let checkbox = $(this).find(".checkbox").val() || "";
                            let clauseType = $(this).find(".clauseType").val() || "";
                            let year = $(this).find(".year").val() || "";
                            let month = $(this).find(".month").val() || "";
                            let day = $(this).find(".day").val() || "";
                            let guaObj = { "code": code, "currencyType": currencyType, "name": name, 
                                "checkbox": checkbox, "clauseType": clauseType, "year": year, "month": month, "day": day };
                            guaArr.push(guaObj);
                        })
                        contractsObj[dataContracts] = guaArr;
                        break;
