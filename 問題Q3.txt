Object.entries(val.guarantor).forEach(([key, value]) => {//授信約定書保證人擔保額度
                    let guaId = value.code
                    let guaCurrency = value.currencyType
                    let guaMoney = value.name
                    $.each($('.guarantor'), function () {
                        let code = $(this).find('.code').val()
                        if (code === guaId) {
                            $(this).find('.guaCurrency').val(guaCurrency)
                            $(this).find('.gccMoney').val(guaMoney)
                        }
                    })
                });

這是暫存後 取出案件的資料
我要加入
checkbox  clauseType  year  month  day
怎麼修改
