        setCreditDefaultSelect($div);
        $div.find("select.address-country").selectpicker({ liveSearch: true });
        $div.find("select.address-country").selectpicker('refresh');
        $div.find("select.address-country").selectpicker('render');

        $div.find('select.nation').val("TH"); 為何這段沒有作用
