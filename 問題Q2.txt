let crptArr = data.result.crptlist;
        let crptArrLen = crptArr.length;

我想把 crptArrLen 傳進以下程式 改怎麼做

try {
            CRRListPage = await Promise.all(crptArr.map((dataObj) => {
                return getUnitCR(dataObj, $target);
            }));
