原先的新增 是他每次會有 baseData.result.crptlist 這1筆資料 根據這筆資料去新增addBorrowerTable(dataRes);

但是我現在是取出 baseData.result.crptlist裡面會有多筆資料 我要如何新增每一筆的 資料到頁面上

//A連動-借保戶多筆資料
//const dataRes = baseData.result.crptlist[0];
//addBorrowerTable(dataRes);
