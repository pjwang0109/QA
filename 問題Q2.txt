

$target 為 id="relInfoTabItem_{num}"

我想從
let $text = $target.closest('#myTabContent').closest('.card-body');
$text.find 每一個 .pageNav 裡面去找 <a class="nav-link active show" href="#relInfoTabItem_{num}"></a>
找到同一個 {num} 之後 再從 <a class="nav-link active show" href="#relInfoTabItem_{num}"></a> 往上取得他的id
該怎麼寫(jquery)
