dataResLength 得到有多少筆資料 我的 ${dataRes[].crpt_custtype}：每一筆資料的crpt_custtype要帶進去 怎麼寫?

const dataRes = baseData.result.crptlist || [];
            const dataResLength = baseData.result.crptlist.length;
            dataRes.forEach(crptlist =>{
                takeOutBorrowerTable(crptlist, dataResLength);
            })


takeOutBorrowerTable(dataRes, dataResLength){
    // let identity = $("#model_identity>option:selected").text();
    crpt_custtype
    console.log("dataRes:", dataRes);
    let num = $(".idBorrowerDiv").length + 1
    const $idDiv = `<div id="idBorrowerDiv_${num}" class="idBorrowerDiv my-2">
              <!-- 1聯徵 -->
              <div class="form-group form-inline creditDiv">
                <span class="idPerson ml-4" style="width:420px">${dataRes[].crpt_custtype}：${dataRes.cname}</span>
.....
