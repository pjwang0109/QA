原本是這樣 <input type="date" name="searchdate_str" class="form-control searchdate_str" /> 

我想改成可以加上時跟分然後他是24小時制的
要怎麼改

----
我得到時、分
要怎麼放到<input type="time" class="form-control searchdate_str_time" name="searchdate_str_time"/>上面
這個目前是有上下午的
    let date = new Date();
    let today = date.toISOString().substr(0, 10);
    const hours = String(date.getHours()).padStart(2, '0');     // 時 
    const minutes = String(date.getMinutes()).padStart(2, '0'); // 分 
