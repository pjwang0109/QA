這是在新增保人時增加的html

function addSurety(){
...
<div class="form-group form-inline my-2">
    <select id="genderList" class="form-control type" onchange="gearingController.suretyGearing(this)">
        <option value="">請選擇</option>
        <option value="1">保證人</option>
        <option value="2">擔保物提供人</option>
        <option value="3">共同借款人</option>
        <option value="4">共同發票人</option>
        <option value="5">票據債務人</option>
        <option value="6">連帶保證人</option>
    </select>
    <input type="text" class="form-control name" placeholder="企業名/姓名"onblur="gearingController.suretyNameGearing(this)"/>
    <input type="text" class="form-control id" placeholder="統一編號/ID" onblur="gearingController.guarantAddField(this)"/>
</div>
...
}


這是他們的JS:
//身分別連動綜合契約書下的負責人部分(身分別的選項對應"："前的文字)
    suretyGearing: function (target) {
        let id = $(target).closest(".identities").attr("id");
        let code = id.substring(10, id.length);
        let typeCht = $(target).find("option:selected").text();
        $(target).closest(".identities").find('.typeCht').val(typeCht)
        console.log("@@身分別更改:", typeCht);
    }



//身分別連動綜合契約書下的負責人部分(企業名/姓名的選項對應輸入框)
    suretyNameGearing: function (target) {
        let id = $(target).closest(".identities").attr("id");
        let code = id.substring(10, id.length);
        let typeChtNa = $(target).val();
        $(`#responsible${code}`).find(".principal").val(typeChtNa);//企業名/姓名
    }



//身分別連動綜合契約書-新增金額與負責人欄位
    guarantAddField: function (target) {
        const $text = $(target);
        const value = $text.val() || "";
        console.log("@@ID統編blur更改:", value);
        if (value != "" && (value.length === 8 || value.length === 10)) {
            // 綜合契約書連動
            suretyGuaGearing(); // 連帶保證人擔保金額
            suretyIDGearing(); // 負責人
        } else if (value.length !== 8 || value.length !== 10) {
            $text.css("border-color", "red")
        }
    }


而這是在重新點選頁面後的身分html:
<div class="form-group form-inline my-2">
    <select id="genderList" class="form-control type" onchange="gearingController.centerIdentityCG(this)">
        <option value="">請選擇</option>
        <option value="1">保證人</option>
        <option value="2">擔保物提供人</option>
        <option value="3">共同借款人</option>
        <option value="4">共同發票人</option>
        <option value="5">票據債務人</option>
        <option value="6">連帶保證人</option>
    </select>
    <input type="text" class="form-control name" placeholder="企業名/姓名"onblur="gearingController.suretyCenterName(this)"/>
    <input type="text" class="form-control id" placeholder="統一編號/ID" onblur="gearingController.guarantCenterChangeID(this)"/>
</div>

重新點選頁面後的身分JS:
//作業中心-更改身分別連動綜合契約書下的負責人部分(身分別的選項對應"："前的文字) suretyGearing
    centerIdentityCG: function (target) {
        let id = $(target).closest(".identities").attr("id");
        let code = id.substring(10, id.length);
        let typeCht = $(target).find("option:selected").text();
        $(target).closest(".identities").find('.typeCht').val(typeCht)
        console.log("@@身分別更改:", typeCht);
    },
    //作業中心-更改身分別連動綜合契約書下的負責人部分(企業名/姓名的選項對應輸入框) suretyNameGearing
    suretyCenterName: function (target) {
        let centerName = $(target).val();
        let id = $(target).closest('.form-group.form-inline.my-2').find('.id').val();
        $('.responsibleDiv .responsible').each(function () { 
            let codeVal = $(this).find(".code").val();
            if (id === codeVal) {
                $(this).find('.principal').val(centerName);
            }
        })
    },
    //作業中心-更改ID欄位-身分別連動綜合契約書-金額與負責人欄位 guarantAddField
    guarantCenterChangeID: function (target) {
        const $text = $(target);
        const value = $text.val() || "";
        let centerName = $(target).closest('.form-group.form-inline.my-2').find('.name').val();
        if (value != "" && (value.length === 8 || value.length === 10)) {
            // 綜合契約書連動
            onblurGuaGearing(); //連帶保證人擔保金額
            onblurIDGearing(); //負責人
            // suretyGuaGearing(); // 連帶保證人擔保金額
            // suretyIDGearing(); // 負責人
            $('.responsibleDiv .responsible').each(function () { 
                let principalVal = $(this).find(".principal").val();
                if (centerName === principalVal) {
                    $(this).find('.code').val(value);
                }
            })
        } else if (value.length !== 8 || value.length !== 10) {
            $text.css("border-color", "red")
        }
    }


主要是讓使用者可以更改centerIdentityCG, suretyCenterName, guarantCenterChangeID

suretyCenterName, guarantCenterChangeID都沒問題
是在於更改centerIdentityCG時
我的身分別需要重新排序 我該怎麼改呢






